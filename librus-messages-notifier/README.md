# Messages watcher for Librus

Setup via crontab with:

    python3 app.py \
        --librus-user 12345 --librus-password PASS \
        --gmail-user xxx --gmail-pass xxxx --rcpt xxxxx,yyyy \
        --state-file /tmp/librus

